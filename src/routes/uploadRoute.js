// import { Router } from "express";
// import { uploadMedia } from "../middlewares/multer";

// export const uploadRouter = Router();

// uploadRouter.post("/", uploadMedia.single("mediaType"), (req, res) => {
//     const mediaUrl = `http://localhost:7890/${req.file.path}`;
//     res.json({ mediaUrl });
//   });

